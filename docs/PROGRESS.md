# 当前状态与恢复入口

更新：2026-09-24。当前为自然语言科研计算服务的开发基础，尚未完成自动计算链路。

Issue #41：候选方案通过冻结条件/生成身份校验，连接已注册评测、资源脚本、已有许可、
受限上传、一次派发和自动跟进，见 [受控执行连接](AUTHORIZED_EXECUTION.md)。上传前后
均核验已有许可，提交前保存不可替换的许可/脚本/策略摘要；上传和派发未知都不自动重试。
服务不签发许可、不自行认定科学检查或独立审查通过，也没有开放网页提交按钮。

298 项本地检查中 297 项通过、1 项 Linux 专用检查跳过。合成模型响应下的实际候选生成、
几何与势函数绑定、文件接收、远端辅助程序的许可检查、字节回收和分析已串联；调度与目标
输出仍为合成数据，LAMMPS 文本从未执行。缺许可、错误证据/资源/输出/脚本、上传时过期、
未知状态和重复调用均有验证。真实许可签发/安装、科学检查、文献输入发布、一次修复、
OVITO、独立评分及首篇完整真实任务仍未完成。当前模型额度仍为 0，没有真实提交。

Issue #39：已派发请求可由同一后台自动跟进调度、核算、回收和冻结计划分析，阶段记录
进入原任务结果页，见 [自动跟进](FOLLOWING.md)。查询机会、间隔及查询回执的存储预留
持久记账；重启不放大额度。失败/中断下载保留副本与费用，转为待核对；已保存报告后退出
可恢复完成状态，不重复计算或下载。服务不创建评测或许可，不提交新作业，也不调用模型。

289 项本地检查中 288 项通过、1 项 Linux 专用检查跳过。新增合成调度→真实本地字节
回收→数值分析→任务结果串联、并发锁、实际进程退出恢复、上限跨重启及失败路径检查。
首轮新增测试的多行上下文语句语法错误已修正。此处只完成提交后的受信自动流程代码；
网页候选到批准提交的整体部署、真实计算、OVITO、科学核验及首篇完整 P-A-B 仍未完成。

Issue #37：任务页加入提交次数、计算/分析状态、数值单位、源行、报告下载与运行历史，见
[结果页面](RESULTS_VIEW.md)。按准备快照定位产品评测，再保留同评测所有尝试；第二次
修复改变快照也不丢记录。未知提交仍占次数，参考角色不进入产品结果。报告与回收回执
经过账本摘要核验后才展示；GET 不触发模型、计算、回收或分析，也不改变提交次数。

272 项本地检查中 271 项通过、1 项 Linux 专用检查跳过；新增测试首轮发生合成准备事务
嵌套锁定，已修正并复测。浏览器独立合成任务已核对数值、单位、源行、历史、2/2 次数与
损坏报告隐藏。正式工作台没有注入合成结果，模型调用与真实目标计算仍为零。结果页只是
保存报告的只读入口；持续执行服务、OVITO、科学核验、首篇完整 P-A-B 和真实部署仍未完成。

Issue #35：同一候选生成路径加入冻结的数值表分析计划，支持显式列单位、计算前确定的
区间、描述统计、末行取值和线性拟合，见 [结果分析](ANALYSIS.md)。分析源码与 Python 版本
由服务一并冻结，执行后更换实现会被拒绝。`AnalysisService`
消费已核验回收包，绑定输入/分析/回收/算法/解释器身份，按原批次预算预留并保存报告。
重复读取复核后复用，其他目录副本另计费；失败保持失败，不能改成科学成功。报告保留
选中源行、单位和样本数，不将时间帧标准差当成独立重复的不确定度。

本地合成链路已贯通冻结方案、真实本地子进程回收、数值处理和不可替换报告。263 项检查中
262 项通过、1 项 Linux 专用检查跳过；首轮集成测试的只读合成文件准备错误已修正。尚无
真实模型/目标计算；单位与物理脚本的一致性、结构分析/OVITO、网页结果页和独立评分仍待
完成。下一步应将执行后状态和结果展现接回用户任务，同时继续处理真实部署与首篇科学条件。

Issue #33：增加 `fetch_outputs` 和一次回收入口，见 [结果回收](OUTPUTS.md)。仅处理终态且
完成核算的账本请求；启动器保存声明输出清单，远端按清单分块传输，本地验证身份、大小、
摘要与执行回执。失败、缺失输出和未完成执行保持原状；已回收不代表物理量有效或科学成功。
每次下载副本和元数据在原批次预算中另行预留，失败/中断副本仍占额度，重试回收不重提计算。
完整结果重复读取会复核并复用；账本保存回收开始/完成及私人回执摘要。

本地 249 项检查中 248 项通过、1 项 Linux 专用检查跳过。新增真实本地子进程传输、工作进程
退出恢复、并发末份预算、断流、路径/链接拒绝、文件变化、回执篡改和诊断保留检查，数据均为
合成字节。尚无真实 HPC 回收；持续计算 worker、网页结果页、自动物理量分析与 OVITO 未接通。
下一步连接科学输出解析和执行后状态流；模型额度、科学条件及真实部署/授权审查缺口仍保留。

Issue #31：扩展引擎清查后发现 3 个 SNAP 版本，取代此前“已查四个均无 SNAP”的部署判断。
15 个已列模块中 13 个帮助查询成功，另 2 个未完成能力核验。一个 2023 年候选的引擎及已解析
依赖约 227 MiB；固定系统解释器后，仅挂载核验文件、隔离外部网络、根目录只读的帮助查询
成功。默认 Python 缺少 memfd 接口，且曾发生一次未定位具体文件的依赖摘要检查失败；失败
记录均保留，不能把后续成功覆盖为始终可用。没有目标模拟、作业提交或真实模型调用。

启动器新增由管理员固定、受签名 profile 摘要约束的库目录与单进程共享内存通信设置，限制
OpenMP/MKL 各 1 线程。不接受模块环境继承、任意环境变量、输入/输出覆盖目录或额外临时
存储。对应版本的 SNAP 源码/文档已静态核对；完整依赖、安装构建来源、计算节点约束、物理
存储配额、身份隔离及科学任务条件仍未验收。下一步应形成受控运行树和部署验收，不能将帮助
查询算成一次成功计算。本地 235 项检查中 234 项通过、1 项 Linux 专用检查跳过；公开文件检查
无发现。远端检查以本分支 PR 当前提交为准。

Issue #29：真实登录端只读探测暴露当前模块缺 SNAP、现有内存检查仅支持 v2 的部署缺口。已核验四个常用引擎模块的帮助输出，均无 SNAP；一项 MPI 初始化失败与后续仅用于帮助查询的共享内存设置均保留私人记录。没有输入目标结构、执行模拟、提交作业或调用模型。登录端配置不能替代计算节点验收。

启动器增加 v1 硬内存上限读取并保留 v2 支持，混合层级使用实际 memory 控制器；拒绝缺失、重复、异常路径与无限限额，不误用软限额或用量。同一读取函数已在登录端成功读取有效上限，不代表计算作业约束已验收。231 项本地检查中 230 项通过、1 项 Linux 专用检查跳过；计算节点部署、带 SNAP 的受控运行树、物理存储配额和正式隔离仍未完成。下一步需解决引擎能力与部署清单，同时补齐科学任务条件及许可签发，不能将只读探测或合成测试计为真实计算通过。

Issue #27：旧 SNAP 参数支持管理员逐资源批准的静态绑定转换，原目录和模型系数不变；记录转换规则、来源、前后摘要与默认值依赖。网页准备、资源筛选与实际绑定共用检查，策略变化阻断旧排队项。全套本地 226 项检查中 225 项通过、1 项 Linux 检查跳过。首次全套测试暴露重复请求与最后模型额度的竞态，已修复并增加确定性复测；重复请求保持同一准备记录。没有真实模型调用或目标计算。

真实铜 SNAP 资源在私人目录完成静态转换核验，原件、系数与许可保留；软件声明是未部署的拟议环境，未声称数值等价。历史 LAMMPS 文档与实现对切换函数默认值的说明不一致，已核对固定版本源码并记录依据。模型生成器的独立 SNAP 设置已核实，但论文补充表参数矛盾、准确调用条件和作者环境仍未解决。新增候选及未选定状态已在实际网页核验；没有合格 A、独立 B 或已复现记录。

Issue #25：方案准备已接入网页和持久历史，见 [准备记录](CANDIDATE_HISTORY.md)。后台分阶段留档，重复请求返回同一意图；通过真实进程退出和活动锁检查区分中断与仍在运行。完成后可下载经过完整性核验的输入、结构、分析说明和准备记录。本地 219 项检查中 218 项通过、1 项 Linux 检查跳过。独立合成浏览器验收核对生成中状态、摘要、历史及服务重启；合成模型请求仍为一次。未产生真实模型或 HPC 调用，正式工作台仍未配置模型服务。

用户已审核产品/测试体系的目标区分并要求继续；已批准的技术路线图作为私人设计依据保存，前一张错误图片排除。产品入口无需论文，文献 P-A-B 用于检验同一执行链路，领域 Adapter、作者源码测试集库及势函数库分别建设。图中的旧误差公式由已审核文字判据取代；社区分享与自定义工具扩展保留为后续范围，不取代前三周验收。

Issue #23：候选生成服务连接冻结研究条件、独立模型、ASE 几何准备、势函数绑定和输入快照，见 [Agent 候选方案](AGENT_CANDIDATES.md)。现有研究任务可进入同一服务；文献测试任务尚未发布给 Agent，继续阻断。模型原始方案、请求及资源记录留存，重复请求不重发。当前未接网页或真实 HPC，科学语义和实际模型能力未验证，所有候选均不具备执行授权。

本轮 209 项本地检查中 208 项通过、1 项 Linux 专用检查跳过；新增真实 ASE 几何/格式检查与合成模型串联检查，未进行任何本地物理计算、真实模型调用或 HPC 提交。ASE 3.29.0 已作为可选依赖安装并核对许可；本机 NumPy 2.5 的上游弃用提示未影响几何测试，依赖版本保存在结构记录内。远端检查以本 PR 当前提交的 CI 为准。

Issue #21：私人势函数资源目录与 `resolve_potential` 领域适配操作已实现，见 [势函数资源](POTENTIALS.md)。固定模型字节、来源、许可和元素顺序，检查单位、SNAP 系数维度及声明的依赖包，并可与现有输入快照衔接。真实 MLEARN Cu SNAP 已私下导入并核对 Git blob，旧参数兼容性问题仍阻断使用；状态仅为已收集。未接通主 Agent、真实计算或自动势函数选择。

本轮 195 项本地检查中 194 项通过、1 项 Linux 专用检查跳过；新增覆盖并发重复导入、版本变化、元素映射、维度不符、旧格式、篡改与输入快照。首次集成测试误用了资源字段名称，修正后重跑通过。原始模型及许可证没有进入公有仓库；没有新增真实模型调用或目标模拟。

Issue #19：DeepSeek JSON 连接、私人请求计数、来源引用校验、条件事务导入及网页入口已实现，见 [模型整理](MODEL_RUNTIME.md)。普通研究不要求论文标识；模型未配置或额度为 0 时不发出请求。真实模型调用、自动科学规划与计算仍未验证，不能将合成响应测试当作模型能力证明。

本轮 179 项本地检查中 178 项通过、1 项 Linux 专用检查跳过；独立合成浏览器验收覆盖保存后自动整理、引用和缺项、额度耗尽及刷新不重调。最初新增测试有一处括号语法错误，已修正后重新通过。生产工作台未配置模型客户端，没有增加真实模型或 HPC 成本。

Issue #17 新增冻结条件的计算任务草稿与参考准备资料分开导出，自动整理自然语言任务，见 [资料导出](TASK_PACKAGES.md)。157 项本地检查中 156 项通过、1 项 Linux 检查跳过；独立合成数据库的浏览器页面已核对。字段分离不构成语义隔离，尚无真实文献任务包验收。运行模型供应商已由用户明确为 DeepSeek；具体模型及密钥尚未配置，调用额度仍为 0。

Issue #15 纠正第一周验收边界：用户提供论文与需求，软件自动整理条件、准备参考、独立生成并执行、分析和比较；用户集中确认与处理真实缺项。开发者手工准备不能计为软件自动化成功。详见 [自动化验收](WEEK1_ACCEPTANCE.md)。运行模型和自动整理进入下一实施入口；本次仅澄清验收，不声称已实现这些功能。

本轮新增文献清单、不可覆盖的关联历史与提交账本只读视图，并调整为灰白科研工作台界面。本地 149 项检查中 148 项通过、1 项 Linux 专用检查跳过；浏览器已核验真实候选清单、分类及历史展开。关联 Issue #13；具体边界见 [文献历史](PAPER_HISTORY.md)，逐项第一周缺口见 [验收核对](WEEK1_AUDIT.md)。仍没有真实目标计算或科学复现结果。

## 当前交付

- [PR 0 / #2](https://github.com/XPYY233/auto-lammps-run/pull/2)：现状审计、三周规范、候选与只读 Zotero 发现。CI 已通过，等待授权维护者审查。
- [PR 1a / #4](https://github.com/XPYY233/auto-lammps-run/pull/4)：受信提交账本、防重复派发、并发核时/存储/次数控制、未知状态、取消及恢复。基于 PR 0 分支；父 PR 合并后应将 base 转向 main。关联 [Issue #3](https://github.com/XPYY233/auto-lammps-run/issues/3)，尚未完成其中的真实 HPC 验证。
- [PR 1b / #5](https://github.com/XPYY233/auto-lammps-run/pull/5)：输入冻结与受控 Slurm 只读查询，基于 PR 1a 分支，继续关联 Issue #3；59 项本地检查通过，远端 CI 以 PR 当前提交检查为准，等待授权维护者审查。
- [PR 1c / #6](https://github.com/XPYY233/auto-lammps-run/pull/6)：分支 feat/pr1c-ledger-reconciliation，基于 PR 1b，继续关联 Issue #3；补齐终态未核算恢复、查询顺序、原子回写和冲突批次阻断。尚无真实计算作业恢复验收。
- [PR 1d / #7](https://github.com/XPYY233/auto-lammps-run/pull/7)：分支 feat/pr1d-restricted-staging，基于 PR 1c，继续关联 Issue #3；受限流式上传、私人接收程序与资源脚本已完成离线验证，未部署真实上传或提交。
- [PR 1e / #8](https://github.com/XPYY233/auto-lammps-run/pull/8)：分支 feat/pr1e-runtime-guard，基于 PR 1d；增加受信提交适配器、签名许可校验、一次性远端派发与计算节点隔离启动器。仅合成验证，尚未部署，见 [RUNTIME.md](RUNTIME.md)。
- [PR 2 / #10](https://github.com/XPYY233/auto-lammps-run/pull/10)：分支 feat/pr2-task-confirmation，基于 PR 1e；持久条件草稿、来源/冲突核对、逐项确认、不可覆盖冻结及本机 FastAPI 网页，关联 [Issue #9](https://github.com/XPYY233/auto-lammps-run/issues/9)。浏览器已实测保存、冲突、刷新、服务重启、冻结和导出；不执行目标计算，见 [TASKS.md](TASKS.md)。
- [PR 2b / #12](https://github.com/XPYY233/auto-lammps-run/pull/12)：分支 feat/pr2b-literature-import，基于 PR 2；来源可定位的文献 CSV 导入与条件冲突保存，关联 [Issue #11](https://github.com/XPYY233/auto-lammps-run/issues/11)。实际边界见 [LITERATURE_IMPORT.md](LITERATURE_IMPORT.md)。
- 主分支保护已读回核实：offline-checks、一次审查、管理员同样受约束；没有自称独立审查或绕过保护。当前 PR 由同一账号发起，GitHub 不允许该账号自行批准；已请求用户提供有写权限的审阅者，尚未修改保护。

## 实际验证

PR 2b 文献条件导入已完成：独立实现旧工作台单条 CSV 格式适配、来源预览、明确输入映射与条目方法分类、事务导入和冻结来源摘要。145 项本地检查中 144 项通过、1 项 Linux 专用检查跳过；浏览器已用单独合成数据库实测预览、映射、冲突撤销、刷新、来源展开、格式错误及重复导入拒绝。没有真实文章导出端到端验收或科学结果，见 [LITERATURE_IMPORT.md](LITERATURE_IMPORT.md)。

PR 2 本机独立环境 135 项检查中 134 项通过、1 项 Linux 专用过滤器测试明确跳过；功能提交 ea20183 的 Linux CI 全部 135 项通过（[记录](https://github.com/XPYY233/auto-lammps-run/actions/runs/35948870109)）。此前 Linux CI 在功能提交 385a9a8 上全部 121 项通过，含真实内核过滤器检查（[运行记录](https://github.com/XPYY233/auto-lammps-run/actions/runs/35946789359)）。检查包括多进程争抢最后一次额度/核时/并发、同请求只派发一次、审计失败阻止派发、真正退出 worker 后不重提、接受回执对账、取消、超支及终态矛盾。提交调度器为合成替身，没有目标模拟。新增输入快照、只读查询解析及真实子进程边界测试。
功能提交 fed8f6a 的 [CI](https://github.com/XPYY233/auto-lammps-run/actions/runs/35886109041) 通过；后续进程退出测试及状态记录由独立提交保存，最终检查以 PR 当前版本为准。

真实 Zotero schema v2 导出通过。一个论文可有多个库条目，因此分别报告条目和 DOI 字符串数并保留重复组，不再使用 unique_papers。数据、条目身份及私人计数只保存在仓库之外，旧导出没有覆盖。

用户指定的登录入口及既有分区只读探测通过。PR 1b 对随机无作业标识完成真实 squeue/sacct 查询，均成功返回空记录，正确保留 unknown/not_visible；未查询其他科研作业详情。该检查仅证明连接/分区查询可用，不证明计算节点依赖或 LAMMPS 执行。没有 SSH 提交、付费 API 或 P-A-B 结果。

## 已批准与未完成

首轮总计 12 核时；单作业最多 8 核/30 分钟/8 GiB；并发 1；存储 1 GiB。模型调用额度为 0。原创代码 Apache-2.0 已批准。具体登录别名和配置只存私有部署记录，不再重复询问这些授权。

输入 manifest、只读查询私人回执、事务化恢复、受限上传及受信提交/启动器代码已实现；真实上传和执行部署、科学静态检查、系统权限隔离验收、模型、网页计算流程和科学评分仍未完成。首篇作者代码与论文条件存在冲突，任务范围及容差尚未冻结。[账本说明](LEDGER.md) 列明当前边界，不能将离线通过称为整个 PR 1 完成。

## 下一入口

下一步补齐科学条件落实检查和保持历史的条件修订，推进首篇作者流程、输入发布及真实部署核验，并贯通执行账本、回收、分析和评分。避免继续扩充手工表单作为最终产品。上一轮已询问具体 DeepSeek 模型与首次最多 5 次调用授权，尚未收到答复；原额度仍为 0，不重复询问，也不擅自调用。

下一步完成首篇作者工作流核验、真实文献条件适配验收与类型化科学条件检查；执行端仍需核实计算节点权限、物理存储强制限制、依赖和隔离验收。[受限上传说明](STAGING.md)列明当前已验证范围与不足。事务恢复与一次性管理员入口见 [RECOVERY.md](RECOVERY.md)。快照和只读查询的边界及来源见 [FROZEN_INPUTS.md](FROZEN_INPUTS.md)。继续核实首篇完整作者流程；满足条件和审查后才在批准预算内计算。

阶段汇报五项：已完成；实际验证；阻塞；需要确认；下一模块。较早审计见 AUDIT.md，第一周范围与候选见 CANDIDATES.md。
