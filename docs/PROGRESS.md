# 当前状态与恢复入口

2026-09-23，PR 0：审计及 Zotero 只读文献发现基础。

已完成：创建新的公有 XPYY233/auto-lammps-run；保留其他项目与私有历史；审计已有 HPC/文献模块；落实开发规则、路线、许可清单和三篇候选；实现本地 API 分页、多层父条目归并、版本漂移检测和仓库外私有导出。

实际验证：13 项离线测试通过；真实 Zotero 检索完整导出成功，版本与计数检查通过。HPC 仅进行 SSH 和调度命令存在性验证。没有模拟、付费 API、P-A 或 P-A-B 结果。私人检索总数和本地路径只保留在本机审计资料中。CI/PR 结果发布后更新。

当前限制：首篇作者脚本与论文方法存在冲突；运行预算、模型协议与范围未冻结；HPC 执行账本、隔离权限、模型、网页、分析与评分尚未实现。现有凭据可达不等于新项目执行授权。尚无独立审查或生产发布。

下一步：先实现 PR 1 的提交前持久账本（原始请求、幂等键、两次限额、预算事务、不明状态对账和重启恢复），再适配已有 Slurm 入口；同时核对首篇作者历史版本。没有预算批准时仅运行离线测试，不执行任何目标能量或受力计算。

阶段汇报固定五项：已完成；实际验证；阻塞；需要确认；下一模块。
